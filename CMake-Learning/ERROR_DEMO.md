# 💣 错误演示：ODR 违规 (链接错误)

我已经修改了代码，现在的状态会导致**构建失败**。

## 发生了什么？

1.  **修改了 `MathLib/MathFunctions.h`**:
    我在头文件里直接写了函数实现：
    ```cpp
    double add(double a, double b) { return a + b; }
    ```

2.  **清空了 `MathLib/MathFunctions.cpp`**:
    只保留了 `#include "MathFunctions.h"`。

## 💥 为什么会炸？

当你运行构建时，链接器会疯掉：

1.  **编译 `MathFunctions.cpp`** -> 生成 `MathLib.lib`
    *   因为它 `#include` 了头文件，所以 `MathLib.lib` 里包含了一份 `add` 的代码。
2.  **编译 `main.cpp`** -> 生成 `main.obj`
    *   你也 `#include` 了头文件，所以 `main.obj` 里**也**包含了一份 `add` 的代码。
3.  **链接阶段 (`target_link_libraries`)**
    *   链接器试图把 `main.obj` 和 `MathLib.lib` 拼在一起。
    *   **链接器报错**：`fatal error LNK1169: one or more multiply defined symbols found` (Visual Studio) 或者 `multiple definition of 'MathLib::add'` (GCC/Clang)。

## 🧪 如何验证？
你现在尝试去运行构建命令（如 `cmake --build .`），一定会看到错误。

## 🚑 如何修复？
要么把实现放回 `.cpp`（标准做法），要么在 `.h` 函数前加上 `inline` 关键字。

## 🖥️ Windows vs Linux 文件格式对比

| 阶段 | Windows (VS) | Linux (GCC/Clang) | 区别点 |
|:---:|:---:|:---:|:---|
| **源码** | `.cpp` / `.cu` | `.cpp` / `.cu` | 完全一样（代码通用） |
| **半成品** | `.obj` (Object) | `.o` (Object) | 名字不同，内容结构类似 (PE vs ELF) |
| **静态库** | `.lib` | `.a` (Archive) | 都是一堆半成品的压缩包 |
| **动态库** | `.dll` | `.so` (Shared Object) | Linux 下没有导入库的概念，直接链接 `.so` |
| **可执行** | `.exe` | 无后缀 (或 `.out`) | Linux 不看后缀，看权限 (是否有 `x` 权限) |

---

## 🔍 核心区别详解

### A. 可执行文件（门禁卡 🎫）

#### Windows (`.exe`)
- **必须**叫 `.exe`，操作系统才承认它是程序
- 例：`my_program.exe`

#### Linux (无后缀)
- Linux **根本不看后缀名**！只看文件属性里的 **可执行权限** (`x` permission)
- 只要赋予权限 `chmod +x my_program`，哪怕叫 `my_program.jpg`，Linux 照样当程序跑
- **默认习惯**：编译通常默认叫 `a.out`，或自己命名为 `main`

---

### B. 库文件（最晕的地方 🌀）

> ⚠️ **这是新手从 Windows 转 Linux 最容易摔跟头的地方**

| 平台 | 静态库 | 动态库 | 需要几个文件？ |
|:---:|:---:|:---:|:---|
| **Windows** | `.lib` | `.lib` (导入库) + `.dll` (实体) | **2 个文件** |
| **Linux** | `.a` | `.so` | **1 个文件** |

#### Linux 动态库工作方式
- **编译时**：链接器直接读 `.so`
- **运行时**：系统直接加载 `.so`
- 无需像 Windows 那样分离导入库和实体

---

### C. 环境变量（找东西的方式 🔍）

| 平台 | 环境变量 | 作用 | 示例 |
|:---:|:---:|:---|:---|
| **Windows** | `Path` | 系统找不到 `.dll` 时，会在 `Path` 里挨个找 | 右键我的电脑 → 环境变量 |
| **Linux** | `LD_LIBRARY_PATH` | 告诉系统去哪里找 `.so` 动态库 | `export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH` |

> 💡 **CUDA 配置提示**：  
> 在 Linux 配 CUDA 环境时，经常要在 `.bashrc` 里写 `export LD_LIBRARY_PATH=...`

---

## 🛠️ 工具链对照（谁在干活？）

| 动作 | Windows 工具 | Linux 工具 | 备注 |
|:---:|:---:|:---:|:---|
| **C++ 编译器** | `cl.exe` (MSVC) | `gcc` / `g++` | `nvcc` 底层会调用它们 |
| **链接器** | `link.exe` | `ld` | 负责拼装 |
| **CUDA 编译器** | `nvcc` | `nvcc` | 这是通用的！ |
| **项目管理** | Visual Studio (`.sln`) | Make (`Makefile`) | **这是两者最大的鸿沟** |
| **跨平台神器** | CMake | CMake | **这就是为什么要学 CMake！** |

> 🎯 **关键启示**：  
> CMake 可以在 Windows 生成 `.sln`，在 Linux 生成 `Makefile`，实现一份代码通吃两个平台！
